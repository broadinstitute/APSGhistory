#c systems
#d Add user $HOME/bin to path.

# Some trickery to make this kit idempotent
# If our op is "use" then do a manual reuse operation.
dk_test $_dk_op == "use" && unuse -q .homebin; use -q .homebin

# If our op was unuse, just manually unuse.
dk_test $_dk_op == "unuse" && unuse -q .homebin

