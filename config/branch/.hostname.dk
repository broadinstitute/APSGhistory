#c systems
#d Set $HOST

# Set the hostname to the short version.
# linux could use hostname -s, but -s means "set" on solaris.
# This should be os agnostic between the two OSes
dk_setenv HOST `hostname | cut -f1 -d'.'`

